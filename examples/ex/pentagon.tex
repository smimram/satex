\deftwocell[triangle]{mu : 2 -> 1}
\[
  \begin{tikzcd}
    & \twocell{(mu * 1 * 1) * (mu * 1) * mu} \ar[dl] \ar[dr] & \\
    \twocell{(1 * mu * 1) * (mu * 1) * mu} \ar[d] & &
    \twocell{(mu * mu) * mu} \ar[d] \\
    \twocell{(1 * mu * 1) * (1 * mu) * mu} \ar[rr] & &
    \twocell{(1 * 1 * mu) * (1 * mu) * mu}
  \end{tikzcd}
\]
