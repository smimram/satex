name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - name: Install packages
      run: sudo DEBIAN_FRONTEND=noninteractive apt-get install ocaml-nox
    - name: Build
      run: make
